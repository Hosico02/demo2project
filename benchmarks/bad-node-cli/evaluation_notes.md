# bad-node-cli — evaluation notes

Vanilla Node CLI with `bin/bad.js` but no scripts, no tests, no docs. The
clearest A/B win for Demo2Project: every gap maps directly to a
RuleBasedExecutor handler, so the demo2project path produces a higher
final score AND a non-lying README, while the baseline path leaves the
README overclaiming.
