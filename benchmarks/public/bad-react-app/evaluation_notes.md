# bad-react-app — evaluation notes

React app missing scripts and config. The `react-app` standard is
auto-selected because the snapshot detects `react` as a framework.
The hardcoded API URL is the latent issue this benchmark hides — neither
path fixes it in v0.0.3, but Demo2Project records a QA case for the
config-hygiene category.
