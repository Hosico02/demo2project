# bad-fastapi-api — evaluation notes

FastAPI fixture. Same Python caveat as `bad-python-cli` — limited
Python-side rule handlers in v0.0.3. Useful for testing standard
auto-selection (`fastapi-api` triggered by `app/main` or `fastapi` framework
detection) and the Dockerfile handler.
