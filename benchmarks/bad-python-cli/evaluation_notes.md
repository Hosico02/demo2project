# bad-python-cli — evaluation notes

This case exists to exercise the Python project path. v0.0.3 has limited
Python-specific RuleBasedExecutor handlers — the README handler still
works, others are deferred. The case is useful primarily for testing
that the Analyzer picks `python-package` standard.
