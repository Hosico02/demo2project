{
  "id": "python-cli-projectization",
  "name": "Python CLI projectization",
  "archetype": "python-cli",
  "description": "Drive a Python CLI demo toward a publishable package (pyproject, tests, Makefile, README).",
  "recommended_standard": "python-cli",
  "default_profile": "conservative",
  "steps": [
    { "command": "analyze", "purpose": "snapshot" },
    { "command": "gap", "purpose": "gap report" }
  ],
  "qa_patterns": ["verification_failure/test_failed", "project_structure_failure/missing_config"],
  "verification_commands": ["pytest -q"],
  "common_risks": ["RuleBasedExecutor lacks Python handlers — see Phase 6 limitation #6", "iterate may add Node-style files; review carefully"],
  "success_criteria": ["pytest runs", "pyproject.toml present"],
  "docs": "docs/guides/analyze-a-demo.md"
}
