{
  "id": "agent-framework-projectization",
  "name": "Agent framework projectization",
  "archetype": "agent-framework",
  "description": "Apply Demo2Project's own playbook to an AI-agent framework repository.",
  "recommended_standard": "agent-framework",
  "default_profile": "balanced",
  "steps": [
    { "command": "analyze", "purpose": "snapshot" },
    { "command": "gap", "purpose": "gap report" },
    { "command": "trust:check", "purpose": "evaluate trust" },
    { "command": "qa:preflight", "purpose": "QA cases" }
  ],
  "qa_patterns": ["process_failure/missing_validation_after_code_change", "process_failure/supervisor_accepted_unverified_result", "executor_failure/output_unparseable"],
  "verification_commands": ["pnpm test", "pnpm build"],
  "common_risks": ["unverified executor claims", "score gaming"],
  "success_criteria": ["score >= 60", "verification gate enforced", "evidence graph present"],
  "docs": "docs/guides/analyze-a-demo.md"
}
